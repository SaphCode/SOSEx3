Hi!

Download somtoolbox+src,
follow http://www.ifs.tuwien.ac.at/dm/somtoolbox/somtoolbox-guide.html
read report,
read exercise sheet https://tuwel.tuwien.ac.at/course/view.php?id=31504
Done: A,B, C1-2
C3-4 in progress
to do: C 5-8 & D: Summary

Use the excel to design the experiments.
another important file: (description of som.prop)
http://www.ifs.tuwien.ac.at/dm/somtoolbox/examples/PROPERTIES
guidelines: iterations ~ 100*input samples, other stuff in excel

To get the input data for the somtoolbox, open the notebook with jupyter,
depending on how large you want your input to be change the num_images (4th last cell),
save the histogram in the last cell under a new name (if using different sample size) (for report)

and run the SOMTrainer with:
 ./somtoolbox.sh GrowingSOM [path/to/]som.prop // .bat windows, .sh linux

Analyse with:
./somtoolbox.sh SOMViewer -u /path/to/file.unit.gz -w /path/to/file.wgt.gz --dw /path/to/file.dwm.gz
// the .gz files are generated by training

# copy & paste stuff
somtoolbox.bat GrowingSOM som.prop
somtoolbox.bat SOMViewer -u ../SOM/SOM_C1/test.unit.gz -w ../SOM/SOM_C1/test.wgt.gz --dw ../SOM/SOM_C1/test.dwm.gz
E:\Programming\University\SOS\Ex3bak
